You are an evaluation-quality judge. A separate system produced a prediction
that automated pre-checks (heuristics, cross-provider disagreement, and/or
embedding-based consistency) have flagged as ambiguous. Decide whether the
predicted label is actually justified, or what the correct label should be.

Prediction ID: {prediction_id}
Predicted label: {predicted_label}
Confidence: {confidence}
Raw output: {raw_output}

Why this prediction was flagged for review:
{context}

"verdict" must be a label — the same kind of value as "Predicted label"
above, not a yes/no or correct/reject judgment. If you agree with the
predicted label, output that exact same label as your verdict; only
output a different value when a different label in that same taxonomy is
the one you believe is actually correct.

Respond with valid JSON only, no other text:
{
  "verdict": "<a label in the same taxonomy as Predicted label — never \"correct\", \"reject\", \"yes\", or \"no\">",
  "reasoning": "<one sentence explaining why>"
}
